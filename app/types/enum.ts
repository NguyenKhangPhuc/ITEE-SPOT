export enum EVENT_STATUS {
    ONGOING = 'ongoing',
    FINISHED = 'finished',
}

export enum INVITATION_STATUS {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected'
}

export enum PROFILE_ROLE {
    ADMIN = 'admin',
    STUDENT = 'student',
    JUDGES = 'judge'
}